<%= link_to "New User", new_user_path %>  

<% @users.each do |user| %>   
             
      <%= user.username %>  
      <%= link_to "Download", user.avatar_url %>  
      <%= link_to "Delete",  user, method: :delete, confirm: "Are you sure you want to delete #{user.name}?" %>    
       
 <% end %>  